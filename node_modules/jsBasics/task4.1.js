/*Завдання 4.1
π - число “пі”. Ви можете використати Math.PI у вашому дз для вираження цього числа

    Створіть змінну radius і присвойте їй числове значення радіуса кола.
    Обчисліть площу кола за формулою π * radius^2 і виведіть результат.*/

//Це вірна формула розрахунку площі кола
let radius = 4;
let ariaCircle = Math.PI * Math.pow(radius,2);
console.log(ariaCircle.toFixed(2));

// Але якщо зробити по формулі з завдання , то вийде інший (не вірний) результат 
let radius1 = 4;
let aria = Math.PI * radius^2;
console.log(aria);